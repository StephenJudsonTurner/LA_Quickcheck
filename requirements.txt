# Tested with the "thermoengine" conda env (Python 3.10). Versions are those present there.
numpy>=1.26
pandas>=2.1
scipy>=1.13      # Akima1DInterpolator(method="makima") needs >= 1.13
openpyxl>=3.1
pytest>=7
